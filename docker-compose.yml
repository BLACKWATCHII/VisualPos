services:
  django:
    build: .
    container_name: django_app
    ports:
      - "8000:8000"
    depends_on:
      - whatsapp
    restart: unless-stopped

  whatsapp:
    build: ./baileys
    container_name: whatsapp_baileys
    ports:
      - "3030:3030"
    restart: unless-stopped
